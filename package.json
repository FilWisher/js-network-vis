{
  "name": "nviz",
  "version": "0.0.1",
  "description": "visualization of a network",
  "main": "index.js",
  "scripts": {
    "test": "tape test/test.js",
    "build": "mkdir -p dist && browserify src/index.js > dist/bundle.js",
    "build:w": "mkdir -p dist && watchify src/index.js -o dist/bundle.js -v",
    "cover": "istanbul cover tape test/test.js",
    "serve": "live-server dist/",
    "build:wb": "npm run build:w &",
    "dev": "npm run build:wb && npm run serve"
  },
  "author": "FilWisher",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^13.0.0",
    "istanbul": "^0.4.2",
    "jsdom": "^8.2.0",
    "live-server": "^0.9.2",
    "tape": "^4.5.1",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "d3": "^3.5.16"
  }
}
